/*let storage = {
          name: '',
          weather: [{
            description: ''
          }],
          main: {
            temp: 0
          },
          wind: {
            speed: 0
          }
        };
        storage = data;
        const weatherItem = new FormItem(storage.name,
                                         storage.weather[0].description,
                                         storage.main.temp,
                                         storage.wind.speed);*/