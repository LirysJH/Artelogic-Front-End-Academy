import React from "react";
import './interface.css';

const interfaceSection = props => {
    return (
        <div className="interface-container">
            
        </div>
    );
};

export default interfaceSection;